#pragma once
// configuration file for Asterlib

#include <string>
namespace Config
{
    std::string const LIBRARY_PATH = "lib\\"; // relative path to get to the lib folder containing all the control and base files TODO: how to do that systematically.
                                              // need a concept, define a folder layout and stick to it
}